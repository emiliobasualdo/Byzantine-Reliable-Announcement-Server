package pt.tecnico.model;

/**
 * Different POST json parameters available
 */
public enum Parameters {
    client_public_key,
    signature,
    action,
    board_public_key,
    number,
    message,
    announcements
}

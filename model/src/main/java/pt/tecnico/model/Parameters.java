package pt.tecnico.model;

/**
 * Different parameters available to the client and the server
 */
public enum Parameters {
    client_nonce,
    server_nonce,
    client_public_key,
    signature,
    post_signature,
    action,
    board_public_key,
    number,
    message,
    announcements,
    err_msg,
    status,
    data,
    broadcast,
    body
}
